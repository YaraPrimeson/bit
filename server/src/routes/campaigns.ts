import {Router} from "express";


const router = Router();

// router.get("/all", campaigns.getCampaigns);
// router.post("/new", campaigns.newCampaign);
// router.patch("/edit", campaigns.editCampaign);
// router.delete("/:id", campaigns.deleteCampaign);

export default router;
